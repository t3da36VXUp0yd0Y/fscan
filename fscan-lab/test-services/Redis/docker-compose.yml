version: '3.8'
services:
  redis:
    build: .
    container_name: redis_test
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - test-network

networks:
  test-network:
    driver: bridge

volumes:
  redis-data:
